This is my old NixOS config. Recently I changed the entire structure of the flake, rewriting most of the stuff, and I couldn't figure out how to merge an orphan branch on top of main without losing its history. That's why this repo will stay here, archived.
Check out [new config](https://github.com/OlegSea/nixos-config).
